/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.pexels.com', 'img.hype.games','blz-contentstack-images.akamaized.net']
    }
  };
  
  export default nextConfig;
  